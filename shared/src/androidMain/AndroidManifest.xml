<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Not required: the editor is fully usable on a device with no camera, and
         marking it required would hide the app from those devices on Play. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application>
        <receiver
            android:name="com.filmroll.camera.notification.DailyReminderReceiver"
            android:exported="false" />

        <receiver
            android:name="com.filmroll.camera.notification.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
